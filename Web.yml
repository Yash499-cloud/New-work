name: SSH-Shell
on: workflow_dispatch # Isse aap manual start kar payenge

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Setup Tmate Session
      uses: mxschmitt/action-tmate@v3
      with:
        limit-access-to-user: ${{ github.actor }} # Sirf aap access kar payenge
