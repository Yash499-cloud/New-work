name: Recovery-Builder-Shell

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Start SSH Shell (Forced Singapore)
        uses: mxschmitt/action-tmate@v3
        with:
          limit-access-to-actor: false
          tmate-server-host: sgp1.tmate.io
          
